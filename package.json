{
  "name": "memoryengine",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "typecheck": "npm run typecheck --workspace web",
    "generate:openapi": "uv run python scripts/export_openapi.py",
    "generate:client": "npm run generate:openapi"
  }
}
